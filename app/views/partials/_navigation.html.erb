<header class="nav-header">
  <%= link_to root_path, class: 'nav-logo-link' do %>
    Bigfoot Marketing
  <% end %>
  <a href="#" class="mobile-nav-toggle"></a>
  <nav class="main-nav">
    <% if user_signed_in? %>
      <%= active_link_to 'Your Projects', projects_path %>
      <% if current_user.is_admin %>
        <%= active_link_to 'Manage Projects', manage_projects_path %>
      <% end %>
      <%= active_link_to 'Log Out', user_logout_path %>
    <% else %>
      <%= active_link_to 'Log In', new_user_session_path %>
    <% end %>
  </nav>
</header>